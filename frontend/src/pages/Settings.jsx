import { useEffect, useState } from 'react'
import { api } from '../api'

export default function Settings(){const [form,setForm]=useState(null),[saved,setSaved]=useState(false);useEffect(()=>{api.get('/settings').then(r=>setForm(r.data))},[]);if(!form)return <p>Loading settings…</p>;const set=(key,value)=>{setSaved(false);setForm({...form,[key]:value})};const submit=e=>{e.preventDefault();api.put('/settings',form).then(r=>{setForm(r.data);setSaved(true)})};return <><div className="page-head"><div><p className="eyebrow">ANALYSIS PARAMETERS</p><h1>Settings</h1></div></div><form className="panel settings" onSubmit={submit}><div className="form-grid"><label>Swing left bars<input type="number" min="1" max="20" value={form.swing_left_bars} onChange={e=>set('swing_left_bars',+e.target.value)}/></label><label>Swing right bars<input type="number" min="1" max="20" value={form.swing_right_bars} onChange={e=>set('swing_right_bars',+e.target.value)}/></label><label>Minimum FVG · ATR fraction<input type="number" step="0.05" min="0" value={form.minimum_fvg_atr_size} onChange={e=>set('minimum_fvg_atr_size',+e.target.value)}/></label><label>Structure confidence<input type="number" step="0.05" min="0" max="1" value={form.structure_confidence_threshold} onChange={e=>set('structure_confidence_threshold',+e.target.value)}/></label></div><label className="toggle"><input type="checkbox" checked={form.wick_break_allowed} onChange={e=>set('wick_break_allowed',e.target.checked)}/><span/>Allow wick-only structure breaks</label><label className="toggle"><input type="checkbox" checked={form.fvg_volume_confirmation} onChange={e=>set('fvg_volume_confirmation',e.target.checked)}/><span/>Require FVG volume confirmation</label><button type="submit">Save configuration</button>{saved&&<em>Saved</em>}</form></>}

